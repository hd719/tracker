import Layout from "../components/Layout";

const About = () => {
  return (
    <Layout>
      <h1>About</h1>
    </Layout>
  );
};

export default About;

// component name does not matter, the file name matter to configure the route
